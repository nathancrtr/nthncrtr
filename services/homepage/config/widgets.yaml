---
# Top-bar widgets shown above the services grid.
# https://gethomepage.dev/configs/info-widgets/

- resources:
    label: System
    cpu: true
    memory: true
    disk: /

- resources:
    label: Media
    # Mounted ro into the homepage container — see docker-compose.yml.
    disk: /mnt/media

- search:
    provider: duckduckgo
    target: _blank
